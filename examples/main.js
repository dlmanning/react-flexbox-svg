import React from 'react'
import ReactDOM from 'react-dom'
import DynamicCollection from './dynamic-collection'

window.React = React

ReactDOM.render(<DynamicCollection />, document.getElementById('app'))

// import StackedItemCollection from './stacked-item-collection'
// ReactDOM.render(<StackedItemCollection />, document.getElementById('app'))
